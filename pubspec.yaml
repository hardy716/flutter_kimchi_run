name: flutter_kimchi_run
description: "A mobile game built with Flutter and Unity WebGL, using game content from a YouTube tutorial by Nicolas from Nomad Coders."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod:
  riverpod_annotation:

  # Routing
  go_router:

  # Data & Serialization
  json_annotation:
  freezed_annotation:

  # Firebase
  firebase_core:
  firebase_auth:
  firebase_storage:
  cloud_firestore:
  firebase_crashlytics:
  firebase_analytics:

  ## Debounce & Throttle
  easy_debounce:

  ## Security
  crypto:

  ## Image
  flutter_svg:
  image_picker:
  cached_network_image:

  ## WebView
  flutter_inappwebview:

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Code Generation
  build_runner:
  retrofit_generator:
  riverpod_generator:
  json_serializable:
  freezed:
  flutter_gen_runner:
  custom_lint_builder:
  analyzer:
  analyzer_plugin:

  # Linting
  flutter_lints:
  riverpod_lint:
  custom_lint:

flutter:
  uses-material-design: true
  fonts:
    - family: Jersey25
      fonts:
        - asset: assets/fonts/Jersey25-Regular.ttf
  assets:
    - assets/fonts/
    - assets/icons/
    - assets/images/

flutter_gen:
  output: lib/gen/
  integrations:
    image: true
    flutter_svg: true
  assets:
    enabled: true
  fonts:
    enabled: true
  colors:
    enabled: true
    inputs:
      - assets/color/colors.xml

