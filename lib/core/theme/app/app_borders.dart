import 'package:flutter/cupertino.dart';

class AppBorderRadius {
  static const BorderRadius box10 = BorderRadius.all(Radius.circular(10.0));
  static const BorderRadius box20 = BorderRadius.all(Radius.circular(20.0));
}